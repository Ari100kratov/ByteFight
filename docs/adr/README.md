# ADR проекта

Этот каталог содержит архитектурные решения, которые затрагивают весь проект, а не один bounded context.

- `0001-production-hosting-topology.md` — production-топология, порядок запуска `migrator`, `web-api`, `chronicles-worker` и инфраструктурных сервисов.

ADR, относящиеся только к `Chronicles`, лежат в `src/Chronicles/docs/adr`.
